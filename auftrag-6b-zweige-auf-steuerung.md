# Auftrag Clinic (Schritt 6b): OP-Bericht-Fallsteuerung aus `OP_STEUERUNG` und Regelsatz statt fester Zweige

Stand 24.09.2026, Cowork-Sitzung. Fortsetzung von Schritt 6a (`abgleich-zweige-6a.md`, Commit eb9de14) und den Entscheidungen des Autors vom 24.09.2026 (`entscheidungen-6a.md` im Projekt; Kurzfassung unten). Datei `app.html`, Ausgangsstand Commit 70d85e7 (806.206 Byte, d3f63960…, Zeilenangaben darauf). Daten: `opsteuerung.json` Stand 3ac (87.582 Byte, d23aec6b…, im Bucket; enthält `HDRG_KOMBI`, `mit`, `drgOhnePartner`, `fraktur_fibula_*`, Arthrorise ohne Hybrid, Lapidus-Hebel 5-784.0u) und der kleine Nachtrag 3ad (`HDRG_KOMBI.partnerEintraege`, Lapidus-Textvariante, siehe Abschnitt 0). Mehrere Commits erlaubt (je Teil einer), eine Vollzugsmeldung je Teil. Keine Datenänderung durch die Code-Sitzung.

## Entscheidungen des Autors (24.09.2026), die dieser Auftrag umsetzt

- **Block 1 B:** Sehnentransfer (5-854.2c) beim Chevron/Scarf/Lapidus und Spongiosaplastik beim Lapidus kommen nur mit gesetztem Chip in die Kodeliste; die festen Pushes in Z. 4997 und Z. 4998 fallen. Grundfall Chevron = Hybrid I20O, Lapidus mit Akin = I20M.
- **Block 1b:** Spongiosa beim Lapidus = 5-783.0v (Entnahme MT I) + 5-784.0u (Transplantation Tarsale); 5-784.0u sperrt die Hybrid über die Fallsperre AKF08 (Band 4, Tabelle V6), nicht über I20-V66/V70.
- **Block 2:** Aufwertung nach I20N und die Kontext-Ziel-DRG I20E gelten nur mit Partnerkode aus der Großzehen-Tabelle (`HDRG_KOMBI.partner`, Definition I20E DH Band 2 S. 1037 f.: „1 Pr aus mind. 2 Tab.: I20-V61; I20-V62"); ohne Partner bleibt I20O bzw. stationär I20F. Betrifft auch den Sprechstundenbrief (heute falsch bei isolierten DMMO 1–2, PIP 2–4, DMMO 3–4).
- **Block 4 A:** Alter unter 18 schließt jede Fuß-Hybrid aus (Kontextfaktor, H-02); im OP-Bericht über den vorhandenen Alters-Chip (`alter === "kind"`, Beschriftung „<18 J. ⚠", Z. 6007); keine Empfehlung „stationär", nur Sachverhalt.
- **Block 6 A:** die drei Zweige ohne Eintrag laufen über `tep_*` (Modifikator `wechsel`) und die neuen Einträge `fraktur_fibula_einfach`/`fraktur_fibula_mehrfragment`.
- **Block 7 A:** die `ziel`-Texte stehen jetzt als `hinweise` in den Daten; der OP-Bericht zeigt Hinweise aus `steuerAnzeige()` wie der Sprechstundenbrief.
- Aus dem Plan: (a) Verdrängung nach Rangzahl statt CM-Reihenfolge, (f) Fallsperre AKF08 vor der Kontextprüfung, Verweildauer-Zeile berechnet, `kodier` stumm (dritter Teil, Punkte 1 und 5).

## 0. Datenvoraussetzung (Cowork, vor Beginn, nach Freigabe des Autors: Nachtrag 3ad)

`HDRG_KOMBI.partnerEintraege = ["chevron","chevron_akin","scarf","youngswick","mtp1_arthrodese","lapidus"]`: Einträge, deren Haupteingriff selbst ein Partnerkode ist (5-788.5c/5e, 5-808.b0, beim Lapidus der Akin-Trigger 5-788.56). Nötig, weil im Sprechstundenbrief `fallCodes` (Z. 3485–3493) die Hauptkodes dieser Einträge nicht enthält (`opsCodes` ist dort null; Kodeliste im Brief wird nicht nachgerüstet, Entscheidung 20.09.). Im OP-Bericht kommen die Kodes aus dem Kodeblock, dort genügt die Kodeprüfung.

## 1. Regelsatz: Partnerbedingung, Fallsperre AKF08, Alter (`hdrgAuswertung`, Z. 1492–1564; wirkt in beiden Generatoren)

1. Neue Eingänge `opts.bestKey` (gibt es), `opts.alter` (`"u18"` | `"ab18"` | null) und `opts.partner` (boolean, vom Aufrufer vorberechnet, siehe 3.). Partner ist erfüllt, wenn mindestens ein Kode in `codes` in `HDRG_KOMBI.partner` steht **oder** `bestKey` in `HDRG_KOMBI.partnerEintraege` steht. Ein Kode zählt nicht als sein eigener Partner (5-788.52 allein ist kein Partner von 5-788.52).
2. `aufwertung`-Einträge mit `mit: "partner"` zählen nur bei erfülltem Partner als Treffer (`aTreffer`, Z. 1507).
3. `kontext`-Einträge mit `drgOhnePartner`: bei nicht erfülltem Partner Ziel-DRG aus `drgOhnePartner[hdrgStart]` statt `drg` (in `hdrgZielDrg`, Z. 1402, oder davor).
4. **Fallsperre AKF08 (f):** vor der Kontextprüfung: steht ein Kode aus `codes` in `HDRG_FALLREGELN.akf08.prozeduren` (40 Kodes), dann `erg.hybrid = false; erg.hdrg = null; erg.sperre = "akf08"`, `erg.drg` = `drgOp` (bzw. das Ziel der zusätzlich getroffenen Kontextregel, höchste nach `DRG_RANG`), Satz: „<Name> (<Kode>) ist ein Kontextfaktor nach Band 4 (Ausschluss Kontextfaktoren MDC 08); der Fall gruppiert stationär nach <DRG>. Ambulant: …" mit demselben AOP-Satzbaustein wie bei Kontextprozeduren (Z. 1521–1526). Kein Wort „Sperre" im sichtbaren Text. `HDRG_FALLREGELN.akf08.diagnosenHinweis` bleibt wie heute ein Hinweis, keine Prüfung.
5. **Alter (H-02):** `opts.alter === "u18"` → `erg.hybrid = false; erg.hdrg = null; erg.sperre = "alter"`, `erg.drg = drgOp`, Satz: „Bei Patienten unter 18 Jahren ist keine Hybrid-DRG möglich (Kontextfaktor Alter); stationär <DRG>, ambulant nach EBM, soweit die Kodes im AOP-Katalog stehen." Keine Empfehlung. Die Alter-Sperre steht vor AKF08 und vor der Kontextprüfung. Im Sprechstundenbrief gibt es kein Alter: dort `alter: null`, Verhalten unverändert.
6. `erg.setting.ambulant` bei `sperre`: `"ebm"`, wenn alle Kodes im AOP-Katalog stehen, sonst `"nein"` (wie heute Z. 1557–1562).

Sprechstundenbrief-Aufruf Z. 3499: `partner` aus `fallCodes` + `bestKey` berechnen (Helfer `partnerErfuellt(codes, bestKey)` neben `hdrgAuswertung`), `alter: null`. Sichtbare Folge im Brief: Metatarsalgie/Zehenfehlstellung + DMMO 1–2 Strahlen → I20O (heute I20N), 3–4 Strahlen → stationär I20F (heute I20E); Kleinzehen-PIP allein 2–4 Zehen → I20O (heute I20N); Chevron/MTP-I + DMMO/PIP unverändert (I20N bzw. I20D/I20C). Die Statuszeilen der Anzahl-Chips („2 Zehen → 5-808.be → Hybrid-DRG möglich", ~Z. 2181–2189) bleiben, sie nennen keine Stufe.

## 2. OP-Bericht: Zuordnung Chips → Steuerungseinträge (neue Tabelle im Code)

Eine Tabelle `OB_STEUERUNG` (Konstante neben `OP_ZUSATZWAHL`), die aus dem Zustand der Chips die Liste der beteiligten Einträge liefert: je Eintrag `{key, mods: [Modifikator-IDs], codes: [zusätzliche Kodes aus Anzahl-Chips]}`. Zuordnung wie `ZUORDNUNG` in `scripts/verify_zweige.py` und `scripts/verify_zweige_6a.py`, ergänzt um die Modifikatoren:

- Vorfuß: `gz` chevron → `chevron` (mit Akin → `chevron_akin`), scarf → `scarf`, youngswick → `youngswick`, cheilek → `cheilektomie`, exostose → `exostose`, lapidus → `lapidus`, mtp1art → `mtp1_arthrodese`; Hoffmann/Tillmann → `mtp1_arthrodese` mit Kodes 5-788.60 (bitte prüfen, ob 5-808.b2 im OPS 2026 noch der gemeinte Kode ist, Zeile 7 der 6a-Liste). DMMO → `dmmo` (Kode nach Zahl `DMMO_CODE`), Weil → `weil`, PIP → `kleinzehen_pip` (Kode nach Zahl `PIP_CODE`, Z. 1640, um `5: "5-808.bh"` ergänzen und für 5 und mehr Zehen verwenden; der heutige Deckel bei vier Zehen im Kodeblock entfällt), Kleinzehen-Weichteil → `kleinzehen_weichteil`.
- Neue Chips (Block 1 B) im Vorfuß-Abschnitt, Zusatzwahl wie `mitAkin`: „Sehnentransfer (5-854.2c)" bei chevron, scarf, lapidus; „Spongiosaplastik (5-783.0v + 5-784.0u)" bei lapidus. Kodeblock Z. 4997: `o.push("5-788.5c","5-788.40","5-93b.0")`, `5-854.2c` nur mit Chip; Z. 4998: `o.push("5-808.a4","5-788.40","5-93b.0","5-93b.e")`, `5-783.0v`,`5-784.0u` nur mit Chip Spongiosa, `5-854.2c` nur mit Chip Sehnentransfer. OP-Text Lapidus: der Satz „Nun Interposition von autologer Spongiosa aus der abgetragenen Exostose …" wird an den Chip gebunden (Textvariante aus 3ad: Baustein `lapidus` ohne den Satz, Zusatzbaustein `lapidus_spongiosa` mit dem Satz, eingefügt an derselben Stelle, wenn der Chip gesetzt ist). Chevron- und Scarf-Text nennen keinen Sehnentransfer; bei gesetztem Chip einen Satz aus `optexte.json` (`zusatz_sehnentransfer_2c`, 3ad) an das Ende des Vorfußtextes anfügen.
- OSG: TEP → `tep_infinity`/`tep_vantage`/`tep_inbone` (+ `wechsel` bei tepType wechsel*), Arthrodesenagel → `arthrodesenagel_retro`, OSG-Arthrodese → `arthrodese_osg` (+ `ohne_spongiosa`, wenn `!osgArtSpongiosa`), AMIC → `amic` (+ `iko`), Brostrom → `brostrom_gould` / `brostrom_int_brace`, Arthrorise → `arthrorise`, ASK-Kodes → `ask_osg`, Supramalleoläre OT → `supramal_valgus`/`supramal_varus`.
- Rückfuß: as_naht → `as_naht`, FHL → `fhl_transfer`, Arthrodesen → `subtalar_arthrodese`/`tn_arthrodese`/`double_arthrodese`/`triple_arthrodese`, Lambrinudi → `hohlfuss_lambrinudi`, MDO → `mdo_mini`/`mdo_offen`, LCOT → `lcot`, Cotton (Rückfuß-Zusatz) → `cotton` (Kodes 5-781.4u + 5-784.7u/0u), haglund_mini → `haglund_mini` (+ Bursektomie 5-859.19 wie heute in `kpAmbZusatz`, jetzt als Kode im Fall), haglund → `haglund_as_split`, os_tib_ext → `os_tib_ext` (+ MDO-Kodes), peroneal → `peroneal_naht`/`peroneal_rek`/`peroneal_lux` (+ `nur_naht`/`ohne_naht`), Coalitio → `coalition_tc`/`coalition_cn` (+ `lcot`, bei Alter unter 12 `lcot_u12`; der Alters-Chip kennt nur „≥18 J."/„<18 J. ⚠", daher `lcot_u12` nur, wenn zusätzlich ein Chip „< 12 J." gesetzt ist, neu neben dem Alters-Chip, nur bei Coalitio sichtbar), Dwyer → `hohlfuss_dwyer`.
- Mittelfuß: TMT → `arthrodese_tmt1` (1 Gelenk), `arthrodese_tmt` (2), `arthrodese_tmt13` (3; 4–5 Gelenke + `vier_fuenf`), Cotton → `cotton`.
- Fraktur: fibula_einf → `fraktur_fibula_einfach`, fibula_mehr → `fraktur_fibula_mehrfragment`, je + `syndesmose` bei `fxSyndes`.
- Metallentfernung → `metallentfernung`, DF → `df_debridement`/`df_amputation`, Achillessehne → `as_tendoskopie`/`as_debridement` (bereits über `steuerAnzeige`, Z. 5376, 5395, 5409; in die Tabelle aufnehmen, Sonderzweige entfallen).

Jeder Chip-Schlüssel, der in der Tabelle fehlt, erscheint in der Konsole als Warnung und im Selbsttest als neue Prüfung „OP-Bericht-Chip ohne Steuerungseintrag" (Hinweis).

## 3. OP-Bericht: Haupteingriff, Kodes, Auswertung (ersetzt die 31 Literal-Zweige, Z. 5108–5420)

1. **Kodes:** `o` wie heute aus dem Kodeblock (Z. 4995–5106), plus die Modifikator-Kodes der Einträge (`modifikatoren[].ops`, abzüglich `opsWeg`). `o` bleibt die Kodeliste des Berichts.
2. **Haupteingriff, Stufe 1 (a):** unter den beteiligten Einträgen gewinnt der mit der besten Rangzahl seiner Basis-DRG (`drg` nach Modifikator). Rangzahlen in einer Tabelle `DRG_RANG_LG` in `opsteuerung.json` (Cowork legt sie mit 3ad an, aus `hybrid-systematik.md` H-03, nur belegte Werte): I20M 102, I20D 103, I27D 114, I20N 119, I20E 120, I27E 129, I59Z 130, I20O 135, I20F 136. DRGs ohne Rangzahl (I20A, I20B, I20C, I13x, I05B, I43B, F27B, F13B) stehen im Baum vor I20D (Struktur S. 411) und gewinnen gegen alle Einträge mit Rangzahl; untereinander nach CM aus `_DRG` (heutige `_candidates`-Logik, Z. 5108–5146, dann nur noch für diese Fälle). Gleichstand: Reihenfolge der Chips.
3. **Auswertung:** `hdrgAuswertung({best: OP_STEUERUNG[key] mit angewandten Modifikatoren, bestKey, hdrg, drg, codes: o, ambulant: true, partner: partnerErfuellt(o, key), alter: alter==="kind" ? "u18" : (alter==="erw" ? "ab18" : null)})`. Stufe 2 (a) ergibt sich daraus: Hybrid nur, wenn `best.hdrg` gesetzt ist und weder Kontext, AKF08 noch Alter greifen. Beidseits wie heute über `beidseitsSperre` (Z. 5427–5431), jetzt mit `o`.
4. **`erloesData`** behält seine Form (Verbraucher Z. 6444–6600): `label` aus `OPS[key].k` plus Modifikator-Labels, `hdrg`/`drg`/`empf` aus der Auswertung und `steuerAnzeige`, `kurz`/`hdrg_n1` aus `erloes2026.json` (`_KURZ`, `UGVD_ABSCHLAG`) statt fester Texte, `ops: o`, `hinweise` aus `steuerAnzeige(key).hinweise` plus Modifikator-Hinweise, `satz`/`warnungen`/`treffer` aus der Auswertung. Die Felder `ziel`, `kodier`, `kpAmbZusatz`, `hebelStat`, `hebelZiel`, `hebelName` entfallen; die Verbraucher (Kodier-Block Z. 6569–6600 mit den durchgestrichenen Kontextkodes, DMMO-REGEL-Block Z. 6597–6599, `erloesText` Z. 5434 ff.) werden durch dieselbe Darstellung ersetzt, die der Sprechstundenbrief für die Auswertung nutzt (Konsequenz-Zeile, Hybrid-Kasten mit Kontext-/Aufwertungs-/Positiv-Listen, Hinweise, Verweildauer-Zeile aus `GVD`/`UGVD_ABSCHLAG`, Z. 3546–3700). Kein Freitext mehr aus dem Code (dritter Teil, Punkte 1 und 5); die Texte „≥3 … keine H-DRG" (Z. 5169, 5173, 5183, 5190, 6599) verschwinden damit.
5. **Selbsttest:** neue Prüfung „OP-Bericht-Zuordnung: Schlüssel ohne `OP_STEUERUNG`-Eintrag" (Fehler) und „Modifikator-ID ohne Eintrag" (Fehler).

## 4. Nichts anderes

Kodeblock (`allOPS`) außer Z. 4997/4998 und dem PIP-Deckel unverändert; OP-Texte unverändert außer den beiden 3ad-Varianten; Materialsatz, Preise, ICD-Block, UC-Bericht unverändert. Sprechstundenbrief nur über Abschnitt 1 (Partner, Alter null).

## 5. Abnahme

Cowork-Prüfstand, drei Läufe: (1) `hybrid_testfaelle.json` (30 Fälle) durch die neue Auswertung (Node/jsc wie in `verify_zweige_6a.py`): Fälle 1–8, 9 (jetzt I20O), 12, 13, 15 (bleibt offen, V6), 16–21, 22/23 (mit `alter: "u18"` keine Hybrid), 24, 26–30 gleich Soll; 30 (FHL + Lapidus + Akin) über AKF08 nach I20D. (2) `verify_zweige_6a.py` gegen den neuen Stand: Spalte A entfällt, Spalte C = Anzeige; bereinigte Liste (`entscheidungen-6a.md` Abschnitt 0) ohne ungeklärte Abweichung. (3) OP-Bericht-Referenz `baseline_ob_kombi_*` und die 17 Kombinationen aus `gp_3ac_ob.py`: Kodelisten unverändert bis auf 5-854.2c/5-783.0v/5-784.0u (nur mit Chip) und PIP ≥ 5; Fallsteuerung gleich bis auf die dokumentierten gewollten Änderungen (Chevron + 3 DMMO → I20E, MTP-I + 4 DMMO → I20C, PIP 2–4 allein → I20O, Lapidus + Akin → I20M, Arthrorise/„< 18 J." keine Hybrid, TMT 4 Gelenke → I20B, Haglund + Bursektomie/Tendoskopie → I27E). Sprechstundenbrief: `baseline_sb_3w.json`-Fälle zeichengleich außer DMMO/Weil/PIP allein (I20O/I20F) und Lapidus-Satz. Selbsttest 0 Fehler, keine Konsolenfehler.

Abnahme des Autors am Handy: OP-Bericht → Vorfuß → Chevron: Fallsteuerung I20O, Chip „Sehnentransfer" setzen → I20E; Lapidus + Akin → I20M, Chip „Spongiosaplastik" → I20D; Mittelfuß → TMT 1–3 + TMT 2 → I20B; Alters-Chip „< 18 J." → keine Hybrid, keine Empfehlung.

Vollzugsmeldung je Teil (1: Regelsatz; 2+3: OP-Bericht) mit Commit-Hash, Zeilennummern und Zählung der Testfälle.

## Nachtrag 24.09.2026: Mechanik der Textvarianten (3ad)

Damit die Daten aus 3ad und der Code aus 6b zusammen ausgeliefert werden können, ohne dass zwischenzeitlich Platzhalter sichtbar werden: `textPlatzhalter()` (Z. 961) bekommt einen dritten Platzhalter `{ZUSATZ:<id>}`. Ist der zugehörige Chip gesetzt (Zuordnung im Code: `lapidus_spongiosa` → Chip Spongiosaplastik, `zusatz_sehnentransfer_2c` → Chip Sehnentransfer), wird er durch `T[<id>]` ersetzt; sonst wird er samt umgebendem Satzrest entfernt, wie heute `{MATERIAL}` ohne Materialsatz (Z. 971–973). Die Daten (3ad, `optexte.json`: Lapidus-Text mit `{ZUSATZ:lapidus_spongiosa}` an der Stelle des heutigen Spongiosa-Satzes, neue Bausteine `lapidus_spongiosa` und `zusatz_sehnentransfer_2c`; Chevron- und Scarf-Text mit `{ZUSATZ:zusatz_sehnentransfer_2c}` vor dem Wundverschluss) werden erst zusammen mit dem Push von 6b in den Bucket geladen (Reihenfolge wie immer: erst Bucket, dann Push). Ein Selbsttest-Eintrag „`{ZUSATZ:…}` ohne Baustein" (Fehler) analog zu Prüfung 5.

## Nachtrag 2, 24.09.2026: Entscheidung des Autors zum Sehnentransfer, Ersetzungsregel

Der Autor (24.09.2026): Die im Chevron-, Scarf- und Lapidus-Text beschriebene armierte und tenotomierte Sehne des M. abductor hallucis **ist** der Sehnentransfer 5-854.2c. Folgen, die Abschnitt 2 und Nachtrag 1 ersetzen, soweit sie davon abweichen:

1. Der Chip heißt „AHL-Transfer (5-854.2c)" und ist bei Chevron, Scarf und Lapidus **standardmäßig gesetzt** (Standardtechnik des Autors). Abgewählt: Satz weg, Kode 5-854.2c nicht in der Kodeliste. Keinen zusätzlichen Satz anfügen; der bestehende Satz ist der Text (kein Baustein `zusatz_sehnentransfer_2c`).
2. Die Daten (3ad, `optexte.json`) tragen `{ZUSATZ:ahl_transfer}` an der Stelle des heutigen Satzes in `chevron`, `scarf`, `lapidus` und `{ZUSATZ:lapidus_spongiosa}` an der Stelle des Spongiosa-Satzes in `lapidus`; die Bausteine `ahl_transfer` und `lapidus_spongiosa` enthalten den jeweiligen Satz **mit abschließendem Leerzeichen**.
3. **Ersetzungsregel** (einfacher als in Nachtrag 1): `{ZUSATZ:<id>}` wird durch `T[<id>]` ersetzt, wenn der zugeordnete Chip gesetzt ist, sonst durch die leere Zeichenkette. Keine Satzrest-Entfernung, keine Leerzeichenbehandlung im Code; die Bausteine sind so geschnitten, dass der Text in beiden Fällen sauber liest (im Prüfstand geprüft: „… partielle Synovektomie. Darstellen der Pseudoexostose …" ohne, „… Synovektomie. Darstellen der Sehne des Muskulus abduktor hallucis, … tenotomiert. Darstellen der Pseudoexostose …" mit Chip). Zuordnung Chip → id im Code: AHL-Transfer → `ahl_transfer`, Spongiosaplastik → `lapidus_spongiosa`.
4. Der Chip Spongiosaplastik beim Lapidus bleibt wie in Abschnitt 2 (Standard: gesetzt, weil der Text des Autors die Spongiosa als Standard beschreibt; abgewählt: Satz und Kodes 5-783.0v/5-784.0u weg). Grundfall Lapidus mit beiden Chips ist damit I20D (AKF08 über 5-784.0u), ohne Spongiosa mit Akin I20M; Grundfall Chevron mit AHL-Transfer ist stationär I20E, ohne I20O. Das ist gewollt: die Fallsteuerung soll zeigen, was die Standardtechnik des Autors ergibt.
